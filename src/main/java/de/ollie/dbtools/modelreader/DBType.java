package de.ollie.dbtools.modelreader;

/**
 * An enum with identifiers for the valid data types.
 * 
 * @author Oliver.Lieshoff
 *
 */
public enum DBType {

	BIGINT, BIT, BLOB, BOOLEAN, CHAR, CLOB, DATE, DECIMAL, INTEGER, LONG, NUMERIC, RAW, ROWID, TIMESTAMP, VARCHAR;

}
# dbtools
A sample of tools and methods for relational database access including database metadata.

## Requirements

* Java 21
* Maven 3.9.9


## Build

Just type `mvn clean install` on CLI in project folder.